// appReducer.js
const initialState = {
    // app-related states
  };
  
  const appReducer = (state = initialState, action) => {
    switch (action.type) {
      // app-related actions
      default:
        return state;
    }
  };
  
  export default appReducer;
  